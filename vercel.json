{
  "buildCommand": "if [ -d Frontend ]; then APP_DIR=Frontend; elif [ -d frontend ]; then APP_DIR=frontend; else echo 'Frontend folder not found'; ls -la; exit 1; fi && cd $APP_DIR && npm install && npm run build && cd .. && rm -rf dist && cp -r $APP_DIR/dist dist",
  "outputDirectory": "dist",
  "functions": {
    "api/index.py": {
      "includeFiles": "Backend/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/((?!api/|assets/|.*\\..*).*)",
      "destination": "/index.html"
    }
  ]
}
